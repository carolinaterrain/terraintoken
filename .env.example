# Supabase Configuration (Auto-generated - DO NOT EDIT)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Admin gating is enforced server-side via the `user_roles` table and
# `AdminRoute.tsx` (Supabase auth + role check). Do NOT add a VITE_-prefixed
# admin password here — any VITE_* var is bundled into the client JS and
# becomes publicly readable.
